# Extra deps for the Week-1 GO/NO-GO feasibility harness (run on the T4 EC2 box).
# Install INTO the same venv you use on the instance:
#   pip install -r requirements-feasibility.txt
#
# IMPORTANT: the default PyPI torch wheel may be a CUDA 13 (cu130) build, which
# does NOT work on this instance's driver (CUDA 12.2). Install a CUDA 12.x build:
#
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# Then install the rest of this file normally:
#   pip install -r requirements-feasibility.txt
# (cu121 = CUDA 12.1, compatible with the T4's 12.2 driver.)

# torch / torchvision are installed via the cu121 index above, not from here.
transformers>=4.57          # Qwen3-VL support landed in recent transformers
accelerate>=0.34            # required for device_map="auto"
bitsandbytes>=0.43          # 4-bit / 8-bit quantization (needs CUDA)
pillow
# Qwen3-VL's processor pulls these in; harmless for MedGemma:
qwen-vl-utils
# (torchvision is installed via the cu121 index together with torch — see header)
